## Hint: Comments

The SAY instruction is wrapped inside a comment block `/* ... */`.

Remove the `/*` and `*/` around the SAY instruction so it becomes
active code.

Then delete the `/* I AM NOT DONE */` line.
